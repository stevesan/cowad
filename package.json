{
  "name": "firebase-rdb-test",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:unit": "vitest run tests/unit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/three": "^0.183.1",
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "three": "^0.183.2"
  }
}
